재귀함수, 즉시실행함수

    재귀 함수
    함수 안에서 같은 함수를 실행(반복)
    [코딩테스트 최단경로 관련 알고리즘 문제 단골]

    fucntion f(n){
        if(n<1)
    }


    즉시실행함수
        실행부 없이 선언부만ㄴ으로 바로 실행
        보통은 익명함수로 주는 경우가 많음.
        재사용이 안되는 일회용 함수임.
        사이트가 열렸을 때 한번만 쓰면 되거나 할 때 씀.

        (function(){
            ... 로직
        })


    화살표함수
        function 키워드 대신(=>)를 사용하여 보다 간결한 방법으로 함수 선언 가능
        ES6,ES7ss


        const a = (param1,param2) =>{

        }

        () => {
            
        }
        1재사용성(파일단위),유지보수,가독성(한 js 내에)
        )
        모듈단위로 js 파일을 통해 분리된 개발이 가능한 컴포넌트 기반의 프론트엔드 프레임웤크
        (Vue,react l...)